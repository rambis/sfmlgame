#include <SFML/Graphics.hpp>
#include "globalHeader.h"

void keyMouseEvents(sf::RenderWindow &window, sf::CircleShape &shape) // Just basic events using keyboards and mouse
{
	float rad = 50.f;	// Radius of circle
	float xPos = 0;		// X-coordinates of circle
	float yPos = 0;		// Y-coordinates of circle

	
}